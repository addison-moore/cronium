# Orchestrator test data

A sample execution payload (`manifest.yaml` + `script.py`) used as a fixture
for manually exercising the runner/executor payload flow. Not part of an
automated test suite; kept as a reference example of the payload layout the
orchestrator produces and the runner consumes.
