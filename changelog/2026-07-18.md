- [2026-07-18] [Fix] Workflow canvas edge labels (user-reported): the connection line ran straight through the condition label text ("On Success", "Always", ...) because Phase 2's tokenization made the label fills translucent tints (`bg-success/10` etc.) — the line showed through except on hover, when the opaque accent hover fill kicked in. Labels now sit on an opaque `bg-background` backing layer (rounded to match), so the line passes behind them; the tinted look is unchanged. Applied to both the workflow-canvas `ConnectionEdge` and the action-builder `ActionEdge`. Also tokenized ConnectionEdge's remaining legacy classes (On Condition purple → primary; Always gray → muted; popover option dots/hovers).
- [2026-07-18] [Fix] Edit Event modal footer (workflows page, user-reported): the sticky Cancel/Update footer in `EventForm`'s modal layout used `bg-background` (the page color — visibly darker than the modal's `bg-popover` surface in dark mode) and a colorless `border-t` (currentColor → white separator in dark). Now `bg-popover` + `border-border`. Swept the app for other colorless directional borders and fixed the two remaining (EventLogsTab and ActivityTable duration summaries).
- [2026-07-18] [Refactor] Monitoring re-imagined as an admin System Health tab (plan: `_plans/monitoring-redesign/PLAN.md`). The standalone Monitoring page, its sidebar entry, and the "Monitoring Access" granular permission are removed; Monitoring is now a tab on the Admin page containing ONLY system health — no event logs or activity feeds (those stay on the Logs page). New `SystemHealth` component: overall status badge, service health cards (database latency, Valkey/Redis cache, memory, CPU — canonical StatusBadge semantics), and a system info card (uptime, platform, Node version, hostname, process-memory and CPU-load bars), auto-refreshing every 30s with manual refresh, skeleton + ErrorState handling. The `monitoring` tRPC router shrank from 7 mixed-audience endpoints (871 lines) to 2 admin-only ones (`getHealthCheck`, `getSystemMetrics`) — getSystemMonitoring/getDashboardStats/getActivityFeed/getExecutionAnalytics/getErrorTracking deleted (only the old page consumed them; Docker healthchecks use the REST `/api/health` route, untouched). Permission cleanup across `RolePermissions` (storage.ts), permissions.ts, the roles-management UI toggle, seed-roles.ts, usePermissions, the admin router role schema, and route types — roles are stored as jsonb so no migration is needed (stale `monitoring` keys are simply ignored). Docs site updated (granular permissions list is now console + localServerAccess; features page mentions the admin System Health tab). Verified: typecheck, lint, 248 tests, clean production builds of both apps.
- [2026-07-18] [Fix] Jobs page brought onto the canonical list-page anatomy (proactive audit after the Workflows/Logs parity fixes): filters were outside the container and the table was double-wrapped in a `<Card>`; now one `bg-secondary-bg` bordered container holds filters + table (skeleton matches), and JobFilters' visible labels became `sr-only` like every other list page. Admin user management intentionally keeps its settings-section structure (it's part of the admin settings pattern, not a standalone list page).
- [2026-07-18] [Fix] Page-structure parity (user-reported): the Workflows page double-wrapped its table — `workflows/page.tsx` put the list inside a `<Card>` while `WorkflowListClient` renders the standard `bg-secondary-bg` bordered container (border-in-border); the page-level Card is removed so Workflows matches Events/Servers exactly. The Logs table's card had a redundant "Recent Activity" header (the page title and tab already say it; no other list table has a header): `ActivityTable`'s `title` is now optional (header row renders only when a title/description is passed — the dashboard's Recent Activity card keeps its title), the logs page passes none, and the header's Refresh button moved into the filter row next to Clear Filters so the affordance is preserved. Verified: typecheck, lint, 248 tests, clean production build.
- [2026-07-18] [Fix] Filter/skeleton consistency pass (user-reported): **(1) Container structure** — the Logs page filters lived in a separate card above the table while Events/Servers put filters and table in one container; `ActivityTable` gained a `filtersSlot` prop and the logs filters now render inside the same card as the table. The Workflows list was missing the shared `bg-secondary-bg` bordered container entirely — added, so all list pages are structurally identical. **(2) Filter surface colors** — `ComboBox` (Tags/Workflow/Event filters) hardcoded `dark:bg-gray-800 border-gray-300`, a different surface from `SelectTrigger`'s `bg-input`; fully tokenized (trigger `bg-input border-border`, popover `bg-popover`, options `hover:bg-muted`/selected `bg-accent`). The Button `outline` variant (Clear Filters, sort-direction, Refresh) used `bg-background` (page color — visibly darker than inputs in dark mode); now `bg-input` so bordered controls all sit on the same surface. **(3) Labels** — logs filters had visible labels while other pages' filters have none; logs labels (both Recent Activity and Workflows tab) are now `sr-only` (kept for screen readers, visually consistent). **(4) Skeleton row dividers** — `TableSkeleton` used bare `divide-y`, which in Tailwind v4 defaults to currentColor (white lines in dark / black in light); now `divide-border`. **(5) Skeleton fidelity** — `DetailPageSkeleton` gained `withTabs` and `variant="canvas"`; workflow detail loads as header+tabs+canvas block, server detail as header+tabs+cards; `(main)/loading.tsx` now mirrors the list-page layout (header + filter row + table in the standard container) instead of a stat-card strip that only matched the dashboard. Verified: typecheck, lint, 248 tests, clean production build.
- [2026-07-18] [Refactor] Styling Phase 5 — the visible polish pass (per `_plans/styling/Review.md`; completes the 5-phase plan). **Dark theme rebuilt as near-black with elevation-by-lightness**: page `#0e1116` → sidebar/cards `#151a21` → popovers/tertiary `#1b212b` → hover fills `#222937`, with monotonic luminance steps — cards now sit visually ABOVE the page (the old theme had them darker, i.e. inverted elevation, on a gray-800 base). Foreground softened to `#eceff3` (less halation than near-white on near-black); dark primary/ring brightened to violet-500 `#8b5cf6`; dark warning/info raised to their 500 shades (`#f59e0b`/`#3b82f6`) for legibility on the darker base. **Light theme cleanup**: background `#feffff` (cyan-cast) → pure `#ffffff`; slate surfaces unified onto the gray ramp (`#f9fafb`/`#f3f4f6`) — one neutral ramp everywhere. **`--accent` fixed**: it's the hover/selected surface token (dropdown focus, list hovers) but was a saturated violet fill with near-black text; now a subtle violet-tinted surface (`#f3eefe` light / `#2a2547` dark) with readable foreground. `--ring` aligned to primary. React-Flow control variables in the app now reference the semantic tokens (one block, both themes) instead of stale hardcoded grays. **Verified accessibility numerically**: 23 WCAG contrast checks pass (4.5:1 text incl. muted-on-all-surfaces and all on-tint status text shades, 3:1 UI colors); smallest interactive hit target is 28px (≥24px, WCAG 2.5.8). **Density/motion**: `TableCell` gets `tabular-nums` so numbers/timestamps align in every table; deleted the dead 700ms `fade-in`/`fade-in-up`/`pulse-slow` keyframes from `@cronium/tailwind-config` (zero usages; remaining animations are all ≤300ms ease-out). Deliberately deferred (need product/asset decisions): true-vector logo rebuild (current logo-icon.svg is a base64 PNG — needs a source asset), sticky table headers (needs per-page max-height design), workflow-canvas keyboard alternatives (feature work, WCAG 2.5.7), and a Cmd+K command palette. Verified: typecheck, lint, 248 tests, clean production builds of both apps, dev smoke with new tokens confirmed in served CSS.
- [2026-07-18] [Refactor] Styling Phase 4 — loading, empty, and error states standardized (per `_plans/styling/Review.md`). Policy: skeletons for content loads, spinners only for user-triggered actions, nothing renders a bare "Loading..." string. New shared pieces in `@cronium/ui`: `DetailPageSkeleton` (page header + content cards mirroring the standard detail layout, `withSidebar` variant) and `ErrorState` (icon + message + retry) — plus `StandardizedTable` now renders a real `TableSkeleton` while loading (was a pulsing "Loading..." text) and the shared `EmptyState` when empty (previously 0 uses; new optional `empty` prop for icon/description/action). Every detail/edit page now shows a skeleton instead of a spinner/plain text/hand-rolled pulse boxes: workflows/[id], servers/[id], events/[id]/edit, servers/[id]/edit, workflows/[id]/edit, admin/users/[id]; jobs/[id] previously blocked with NO loading UI — its content now streams behind Suspense with the skeleton. Suspense-bypass fixes: `DashboardStats` renders `DashboardStatsSkeleton` client-side on first load (was zero-value stats) and `ErrorState` with retry on failure (was a raw red paragraph); `EventDetails` renders `EventDetailsSkeleton` while fetching (was Spinner+text, so the RSC fallback and client load now match); `ActivityTable` uses `TableSkeleton` + `EmptyState`. Purpose-built skeletons adopted where ignored: jobs list uses `JobsTableSkeleton`; admin users table gets skeleton rows + `EmptyState` (was "Loading users..." text); `(main)/loading.tsx` now mirrors the dashboard layout (header + stat strip + content card). Not-found unification: the four hand-rolled Not Found layouts (server, workflow, admin user, log) now use `NotFoundCard`. Accessibility: all 45 `animate-spin`/`animate-pulse` usages across the app and `@cronium/ui` (including the base `Skeleton`) are now `motion-safe:` gated, honoring `prefers-reduced-motion` (WCAG 2.3.3). Verified: typecheck, lint, 248 tests, clean production build, dev smoke test.
- [2026-07-18] [Refactor] Styling Phase 3 — one page shell, one header, one icon convention (per `_plans/styling/Review.md`). New `PageShell` component in `@cronium/ui` (centered container + `space-y-6` rhythm; `width="full"` variant available): all ~25 dashboard pages, client shells (logs, monitoring, event details), skeletons, and route-group `loading.tsx` files migrated onto it, replacing five competing wrappers (`container mx-auto p-4`, `py-6`, `py-8`, `space-y-8`, and the full-width `flex-1 space-y-4 p-4 pt-6 md:p-8` shadcn shell on jobs/api-tokens). Page padding is now owned once by the dashboard layout's `<main>` (ends the double-padding defect — pages no longer add their own). `DashboardHeader` deleted; its three pages use the shared `PageHeader`, and page titles are normalized to `text-2xl font-semibold` (was a mix of xl/2xl/3xl/4xl bold; dashboard/admin/monitoring/workflow-edit h1s downsized accordingly). Radius drift fixed: 42 bare `rounded` (4px) class tokens swept to `rounded-md` (the 6px `--radius` baseline); deliberately did NOT convert framed table/code regions and interactive list rows to `<Card>` — Card's shadow+tinted bg would change their design (the audit's "52 ad-hoc card divs" are mostly these). Icons: 🔒/💡/✕ emoji glyphs replaced with lucide `Lock`/`Lightbulb`/`X` (ServerForm, EventForm, ActionSelector, HttpRequestForm); 39 numeric `size={N}` props on lucide icons converted to the h-/w- class convention (custom `EventTypeIcon`/plugin-icon numeric APIs intentionally kept); `google-sheets-icon` strokeWidth normalized 1.5→2; deleted the unreferenced duplicate brand SVGs in `public/assets/integrations/logos/`. Verified: typecheck, lint, 248 tests, clean production build, dev-server smoke test.
- [2026-07-18] [Fix] Status-badge stragglers (Phase 2 follow-up): several surfaces still rendered status through the generic `Badge` `default` variant, producing purple "Active"/"SUCCESS" pills that mimicked StatusBadge with the brand color. Migrated to the canonical `StatusBadge`/`StatusIcon`: Tools page Health Overview (tool Active/Inactive) and Execution History (log SUCCESS/FAILED, now proper-case labels + matching row icon), `ToolHealthIndicator` (both the inline indicator and the compact icon-only health pill now derive from `getStatusConfig`), MonitoringClient service Healthy/Down, JobExecutionLogs Connected/Disconnected, EventDetailsTab Active/Inactive, and ActionEdge's success/failure edge labels (tinted instead of solid purple). Added `inactive`/`healthy`/`unhealthy`/`checking`/`unknown`/`connected`/`disconnected` to the central status map; tokenized `Badge`'s solid `success`/`warning` variants (were `text-white hover:bg-green-600`-style raw classes); Teams/Google-Sheets "connected" chips use the standard success tint. Verified: typecheck, lint, 248 tests, production build green.

# Changelog - 2026-07-18

- [2026-07-18] [Refactor] Styling Phase 2 — canonical status semantics (per `_plans/styling/Review.md` §4). `status-badge.tsx` is now the single status module: a 6-theme table (success/destructive/warning/info/neutral/brand) built purely on semantic tokens, a status→theme+icon map covering event/log/user/job statuses plus lowercase aliases, and a new exported `StatusIcon` for icon-only surfaces. Canonical mapping enforced everywhere: success=green `CircleCheck`, failure=red `CircleX` (was variously XCircle/AlertCircle/AlertTriangle), running=blue `Loader2` spinning (was RefreshCw/Clock), pending/draft=gray static `Clock`, warning/paused/timeout/partial=amber (collapsing the yellow/amber/orange split), info=blue (resolving the indigo-badge vs blue-token collision). Migrated the ad-hoc status renderers to it (JobsTable, JobStatusCard, WorkflowExecutionGraph, action-builder PreviewPanel/ActionNode, WorkflowsCard, EventOverviewTab, DashboardStats, events-table run button) and tokenized the React-Flow canvases — ConnectionEdge/ActionEdge/Canvas/WorkflowCanvas hex literals became `var(--success-color)` etc., so edges/minimap/grid now follow dark mode (ConnectionEdge also dropped its `useTheme` dependency). Added status _on-tint text_ tokens (`--success/warning/destructive/info-text-color` + light/dark values) for readable label text on translucent fills, and brightened dark-mode `--destructive` from #b91c1c to #ef4444 (shadcn-style; the old value had ~2.5:1 text contrast on dark). Swept raw status-hue palette classes app-wide (error panels, form errors, filter dots, bulk-action buttons, auth pages, admin notifications): status-meaning raw classes reduced from ~600 occurrences to 20, all brand/terminal-specific. Verified: typecheck, lint, 248 tests, and production builds of both apps green; new utilities confirmed present in compiled CSS.
- [2026-07-18] [Fix] `next build` static export: removed `NODE_ENV=development` from `env/.env.local` and both env templates (the app's dotenv-wrapped build script leaked it into the export workers, which then loaded React's _development_ edge runtimes against Next's compiled production pages runtime — crashing /404 & /500 prerender with "<Html> should not be imported outside of pages/_document" and client components with a null hooks dispatcher). Next.js manages NODE_ENV itself; `env.mjs` now defaults NODE_ENV to "development" for processes launched outside the Next CLI (socket server via tsx). Combined with the @anthropic-ai/sdk externalization, `pnpm build` for cronium-app fully passes again.
