- [2026-07-17] [Refactor] Styling Phase 1 (design-token foundation, per `_plans/styling/Review.md`): the semantic design tokens (`:root`/`.dark` color values, single `--radius` with all radius steps derived from it, shadows, font stacks, shared animations) now live once in `packages/ui/src/styles.css` and are consumed by both apps — deleting the ~330 duplicated token lines from each app's `global.css`, the conflicting second radius scale, and cronium-info's parallel `light-dark()` theme layer (info's `ThemeProvider` already toggles `.dark`, and its pre-hydration script now also honors the stored theme choice). Added the missing `info`/`secondary-bg`/`tertiary-bg` tokens to the shared mapping. Dismantled the legacy global-CSS component system in `components.css` (`.btn`/`.alert`/card/toast blocks and the element/attribute selectors that restyled every `nav`/`header` and Radix `[role=menu]`/dialog): toast variant colors moved into `toast.tsx` CVA variants, sidebar/nav-item styles became Tailwind utilities in `layout-client.tsx`, and dropdown/popover/dialog surfaces now carry semantic tokens themselves (`bg-popover` etc. — dropdowns previously had NO background of their own outside that global CSS; dark dialogs shift from slate-950 to the standard popover surface). `DialogContent` gained `showCloseButton`, fixing Modal's double close button; `DialogDescription` and the dialog close hover use `text-muted-foreground`/`bg-muted`. Logo wordmark color is CSS-driven (`text-primary dark:text-secondary`), fixing the never-switching dark logo on cronium-info (it used next-themes' `useTheme`, which info doesn't provide). Inter is now wired through the `--font-sans` token via `next/font` in BOTH apps (info previously rendered the system stack). lucide-react aligned to ^0.552.0 workspace-wide; app tailwind config uses `presets: [sharedConfig]`. Deleted dead/duplicated code: app-local byte-copies of `@cronium/ui` components (`table`, `standardized-table`, `table-skeleton`, `action-menu`, `monaco-editor*` — ActionMenu imports repointed to `@cronium/ui`), dead `WorkflowList.tsx`, six dead error/streaming fallback components, unused `LoadingIndicator`/`LoadingState`, unused utilities.css classes, and nine redundant per-usage `SelectContent` style overrides. Verified: typecheck + lint + 248 app tests green, info production build green, fresh dev server renders `/` and `/auth/signin` 200 with legacy selectors absent and token-derived utilities present in the compiled CSS.
- [2026-07-17] [Fix] next build: externalize `@anthropic-ai/sdk` from the client webpack trace (broken since the AI tool plugin landed — `ai-plugin.tsx` → `connection-test` → `lib/ai.ts` reaches `node:path` in the client bundle; CI doesn't run `next build`, so it went unnoticed). Uses the object/commonjs externals form because bare-string externals emit `module.exports = @anthropic-ai/sdk`, which is unparseable for scoped names and broke minification. NOTE: `next build` still fails afterwards at static prerender of `/404` (`<Html> should not be imported outside of pages/_document`) — verified pre-existing on the parent commit too (not from the styling refactor); needs a separate investigation.
- [2026-07-17] [Docs] Documented the MCP event/workflow creation feature: new `/docs/mcp` page on the marketing/docs site (`apps/cronium-info`) — overview, the SQL→Slack@8am example, remote (claude.ai OAuth connector) vs local (Claude Desktop/Code stdio) setup, the tool list, auth + "Limit to MCP" scopes, and the drafts/approval/provenance safety model — plus a docs-nav entry (API Reference → "MCP (AI Agents)"), a docs-landing card, and a sitemap entry. Added a contributor-facing `docs/MCP.md` (architecture: the two servers, discovery/validate/create flow, OAuth 2.1 server, token scopes, provenance, key files) and linked it from `docs/README.md`. Info-site production build verified (page renders as static).
- [2026-07-17] [Feature] MCP hardening — provenance, audit & dry-run validate (Phase 4). **Provenance:** new nullable `source` column on `events` and `workflows` (`null` = UI/normal, `"mcp"` = created by an AI agent), applied via `db:push`. `ctx.requestSource` is set from an `x-cronium-source: mcp` header (sent by the stdio MCP server) or directly by the in-process `/api/mcp` route; `events.create`/`workflows.create` stamp it and emit a `[MCP-AUDIT]` server log line. Only the known value `"mcp"` is honored (clients can't stamp arbitrary text). **Dry-run validation:** new `mcp.validatePlan` (added to the `mcp` scope) checks a proposed plan — events keyed locally + an optional workflow chaining them by key — **without persisting**: event schemas (`createEventSchema`), `toolActionConfig` (toolId ∈ the user's credentials, actionId/params against the tool registry), and the workflow graph (unknown keys, self-loops, fan-in, cycles). Exposed as a `validate_plan` tool on both MCP servers so the agent can validate a draft before showing it for approval. Verified live end-to-end (7/7): validate_plan accepts a valid plan and flags unknown toolId / cycle / fan-in; events + workflows created via MCP carry `source="mcp"`. typecheck + lint clean; full app suite green (248 tests).
- [2026-07-17] [Feature] MCP per-token scopes (Phase 4): API tokens and OAuth access tokens can now be least-privilege. New nullable `api_tokens.scopes` column (jsonb `string[]`; `null` = full user rights, so every existing token is unchanged). A scope catalog (`src/server/token-scopes.ts`) maps the `mcp` scope to exactly the tRPC procedure paths the MCP tools need (create/activate/read events & workflows, `mcp.getCapabilities`, `tools.getAll`, `servers.getAll`) and nothing else. `authenticateApiToken` returns the token's scopes; `createTRPCContext` and the `/api/mcp` route thread them as `ctx.tokenScopes`; a new `enforceTokenScopes` middleware on `protectedProcedure` (inherited by `adminProcedure`) rejects any procedure a scoped token doesn't permit with `FORBIDDEN`. OAuth access tokens carry their granted scope (`mcp`) through the same path, so an OAuth connector is inherently limited to MCP operations. Cookie-session users and unscoped tokens remain unrestricted. `auth.createApiToken` accepts an optional `scopes` array; the API Tokens page (`ApiTokensManager`) gains a "Limit to MCP" toggle and shows a scope badge on scoped tokens. Verified live end-to-end (7/7): a full token is unrestricted, an `mcp`-scoped token is allowed on the MCP paths + `/api/mcp` but 403 FORBIDDEN on `auth.getApiTokens`; plus 5 unit tests for the allowlist. Additive schema change applied via `db:push`. typecheck + lint clean; full app suite green (248 tests).
- [2026-07-17] [Feature] MCP OAuth 2.1 authorization server (Phase 3b): Cronium now serves the OAuth 2.1 authorization-code + PKCE flow for the `/api/mcp` resource, so the claude.ai connector's default OAuth works (Connect → sign in → approve; no token to paste) — not just a static bearer. Self-contained: HMAC-signed access/refresh tokens keyed off `AUTH_SECRET` (no JWT dep) + Valkey-backed clients/codes/refresh (no DB migration), all under `src/lib/mcp-oauth/` and `src/app/api/mcp/oauth/`. Discovery: RFC 8414 authorization-server metadata + RFC 9728 protected-resource metadata (handlers under `/api/mcp/oauth/metadata/*`, exposed at `/.well-known/oauth-authorization-server` and `/.well-known/oauth-protected-resource[/api/mcp]` via `next.config.mjs` rewrites); `/api/mcp` 401 now carries `WWW-Authenticate: Bearer resource_metadata="…"`. RFC 7591 dynamic client registration (public clients, PKCE; redirect_uris limited to HTTPS/loopback). Authorize endpoint validates client + exact redirect_uri + S256 PKCE, authenticates the resource owner via NextAuth session or a Cronium API token (headless), and shows a consent screen whose Approve carries a signed ticket bound to the user + params (CSRF-safe). Token endpoint: single-use codes (Valkey) + PKCE verify → audience-bound access token (1h) + rotating refresh token. `/api/mcp` accepts an OAuth access token or a raw API token. Kept in a separate namespace from the existing `/api/oauth/*` (outbound tool OAuth). Verified live end-to-end (13/13: discovery, 401 metadata pointer, DCR + non-loopback rejection, consent, code+PKCE exchange, OAuth-token-authorizes-/api/mcp, single-use code, refresh rotation, PKCE-mismatch rejection) plus 9 unit tests for the token/PKCE/ticket crypto. typecheck + lint clean; full app suite green (243 tests). `/.well-known/*` rewrites activate on server restart.
- [2026-07-17] [Feature] MCP remote endpoint (Phase 3a): new in-process `/api/mcp` route (`apps/cronium-app/src/app/api/mcp/`) serves the same five MCP tools over **Streamable HTTP** (stateless JSON-RPC, JSON-response mode) so the claude.ai web app — and any HTTP MCP client — can create Cronium events/workflows. Handlers call the app router via `createCaller` in-process (no HTTP hop, no create-schema duplication); tool input schemas exposed as JSON Schema for `tools/list`. Auth is a strict API-token bearer via the now-exported `sessionFromApiToken` — enforced directly in the route (NOT the dev auto-auth fallback), so unauthenticated and bogus tokens get `401` even in development (claude.ai "custom header" connector auth; any client sending `Authorization: Bearer <token>`). `POST` handles initialize/notifications/ping/tools.list/tools.call (single or batch); `GET` → 405; `OPTIONS` → 204 + CORS. Verified live end-to-end (8/8): no-token + bogus-token → 401, initialize, tools/list, get_capabilities, and create_workflow_bundle over HTTP creating + persisting a workflow (read back, cleaned up). OAuth 2.1 for public/multi-tenant hosting is the documented next phase (3b); README updated with the remote-endpoint + claude.ai setup.
- [2026-07-17] [Feature] MCP support (Phase 1–2): generate Cronium events & workflows from an AI app. **tRPC now accepts API-token bearer auth** — `createTRPCContext` (`src/server/api/trpc.ts`) falls back to `Authorization: Bearer <api-token>` (via new `getBearerToken`/`authenticateApiToken` helpers in `src/lib/api-auth.ts`, respecting ACTIVE status + expiry) and synthesizes a session from the token's user, so headless clients reach the full existing create surface (`events.create`/`events.activate`/`workflows.create`/`tools.getAll`) with no schema duplication; cookie sessions still take precedence. New read-only **`mcp.getCapabilities`** router returns enums, defaults, scheduling guidance (specific times need a `customSchedule` cron; `events.create` doesn't auto-register the scheduler → `events.activate`), each tool type's actions + parameter metadata, and the user's real tool-credential and server ids — everything an agent needs to draft valid payloads. New **`apps/cronium-mcp`** package: a local stdio MCP server (Claude Desktop / Claude Code; no OAuth) that authenticates with a Cronium API token and exposes `get_capabilities`, `create_event`, `activate_event`, `create_workflow`, and `create_workflow_bundle` (creates events as DRAFT + a workflow chaining them by key, best-effort rollback), with a hand-rolled superjson tRPC client and a README (Claude Desktop/Code setup). Events land as DRAFT so nothing runs until the user reviews/activates. Tests: 8 unit tests for the token-auth helpers; the stdio server passes a real initialize + tools/list handshake. Remote/OAuth (claude.ai web) and per-token scopes are documented follow-ups (`_plans/mcp/PLAN.md`, Phases 3–4).
- [2026-07-17] [Feature] AI tool actions: new `ai` tool plugin (`src/tools/plugins/ai/`) lets an event or workflow step prompt an LLM and pass its reply to the next step via Unified I/O. LLM-agnostic with **per-user connections** — each user adds their own encrypted AI connection (provider + key + optional base URL + default model) on the Tools page, exactly like Slack/SQL, covering OpenAI, Anthropic (Claude), Google Gemini, and any OpenAI-compatible endpoint (Groq/Together/OpenRouter/Ollama/vLLM). One `ai-prompt` action (system/user prompt template-able with `{{cronium.input.*}}`, model, temperature, max tokens) with a `text | json` response-format toggle: text mode emits `{ text, model, provider, usage }` (downstream reads `{{cronium.input.text}}`); JSON mode parses the reply (strips code fences) and spreads the object at top level with metadata under `_ai`, so downstream steps read typed fields / branch on them. Reuses the existing multi-provider layer: refactored `lib/ai.ts` to add a provider-agnostic `generateText(config, opts)` (explicit config, not system settings) shared with the script assistant, honoring the event timeout (AbortSignal), JSON mode (native where available + prompt instruction), and the 5 MB Unified I/O cap. Connection test validates the key via the provider's model list; a new `ai.listModels` protected tRPC endpoint powers the per-user model picker (the existing one was admin-only). Registered in the server manifest registry and client plugin registry; no DB migration. Tests: 12 unit tests (action config/model-resolution/JSON parsing/failure paths + registry wiring). Docs site tool list and `docs/tools/AI_TOOL.md` added.
- [2026-07-17] [Feature] AI Assistant multi-provider support: admin can choose OpenAI, Anthropic (Claude), Google Gemini, or any OpenAI-compatible endpoint (Kimi/Qwen/Llama via Groq, Together, Ollama, etc.); model dropdown now lists live models fetched from the selected provider (curated fallback when unreachable) instead of a hardcoded list of retired OpenAI models; per-provider API keys encrypted at rest; docs site updated
- [2026-07-17] [Fix] Add missing @types/nodemailer devDependency to cronium-info (typecheck was failing after nodemailer 9 bump)

# Changelog - 2026-07-17

- [2026-07-17] [Refactor] Removed the Conditional Actions feature (see `_plans/conditional-actions/`). Workflows already cover the same branching — on-success / on-failure / on-condition edges, fan-out, scheduling, and both "run another event" and "send a notification" — and the two features silently compounded (an event used as a workflow step fired its own conditional actions in addition to the workflow's edges). With no users and no production data, the whole feature was removed rather than deprecated. Dropped: the `ConditionalActionsSection.tsx` form, the `conditional_actions` table + `ConditionalActionType` enum + relations, the storage methods, the single execution dispatch site (`event-handlers.ts` and the hook in `job-service.updateJobStatus`), the CA blocks from the events tRPC router and both REST routes, `event-validation.ts` (the CA-only cycle guard), and the `isSendMessageAction`/`conditionalActionConfig` metadata from the Slack/Discord/Email plugins (those send actions keep working as tool-action events and as workflow steps). Also removed ~1,500 lines of now-dead code the feature had accreted (`status-badge-renderer.tsx`, the isolated `useEventForm`/`types`/`guards` cluster). Deleting the feature also erased a large latent-bug surface, including one action type (`ON_CONDITION`) that had never worked and a `deleteActionsByEventId` bug that duplicated rows and destroyed other events' actions on every edit. The `conditional_actions` table was dropped from the dev DB (the app applies schema via `db:push`, not migrations). Docs updated across the app docs and the `cronium-info` site (the `/docs/conditional-actions` page and its nav/sitemap/index links removed; guidance repointed at Workflows). Net: ~3,800 lines removed, one table dropped, two overlapping branching systems collapsed into one. Verified: typecheck + full-app lint clean, 154 tests passing, both app and info-site production builds compile, and the `conditional_actions` FK to `events` is confirmed gone in the dev DB.
- [2026-07-17] [Feature] MongoDB tool: new `mongodb` tool plugin (`src/tools/plugins/mongodb/`) mirroring the SQL tool's design. Five actions — Find Documents and Aggregate Documents (reads; results flow to the next workflow step via Unified I/O, streamed with the same fail-don't-truncate row/byte guards as SQL, `$out`/`$merge` rejected in pipelines) plus Insert/Update/Delete Documents (explicit writes; mass update/delete with an empty filter is refused, delete always requires a filter). Structured credentials (standard or `mongodb+srv`/Atlas scheme, host, port, database, user, password, auth source, TLS mode) with the `mongodb` driver imported dynamically and one short-lived connection per call; Extended JSON (`$oid`, `$date`) supported in filters/documents; BSON values (ObjectId, Decimal128, Long, Binary, dates) normalized to JSON-safe output. Registered in the server manifest registry and client plugin registry. Tests: 43 unit tests (helpers + all five actions with a mocked client) plus a gated live-integration suite (`CRONIUM_MONGO_TEST_URL`) verified against MongoDB 7 in Docker. Docs site tool list updated.
- [2026-07-17] [Fix] AI settings: saving credentials failed with a 400 — numeric-looking stored settings (smtpPort "587") round-trip through getSystemSettings as numbers and were rejected on save; now coerced server-side and accepted by the schema
- [2026-07-17] [Fix] AI settings: refreshed fallback model catalogs to current lineups (GPT-5.6 Sol/Terra/Luna, Claude Fable 5 + Opus 4.8/Sonnet 5/Haiku 4.5, Gemini 3.5/3.1/2.5) and surfaced the provider error (e.g. invalid API key) in the UI instead of silently showing the fallback list
- [2026-07-17] [Fix] Select dropdowns (shared @cronium/ui component) now cap their height to the viewport and scroll (Radix available-height max plus scroll buttons) — long lists like the live AI model list were overflowing off-screen with no way to reach lower items
- [2026-07-17] [Chore] packages/ui: add missing eslint devDependency (lint was failing with command-not-found after the pnpm 11 migration) and reformat components to the current prettier-plugin-tailwindcss class order
- [2026-07-17] [Fix] Archived servers no longer appear in default server listings: `storage.queryServers` (behind `servers.getAll`) now excludes `is_archived` rows, so the servers table's default/"All Statuses" view, the event form's Execution Settings server list, the console server picker, the workflow form, and the server-groups tab all show active servers only. The "Archived" status filter still lists them via the dedicated `servers.getArchived` endpoint.
- [2026-07-17] [Fix] Tools page build error ("Can't resolve 'timers/promises'"): externalized the `mongodb` driver in next.config.mjs (webpack `externals` + `serverExternalPackages`), matching the existing `mysql2` pattern — the driver is only ever loaded via server-executed dynamic imports, and externalizing keeps it out of the client webpack trace. Verified with a clean production build.
- [2026-07-17] [Improvement] AI Script Assistant is now Cronium-aware: the system prompt teaches the runtime helper API per language (Node global cronium with await + async IIFE, Python snake_case cronium module, Bash cronium_* functions) covering input/output, get/setVariable, setCondition, and event, with the remote-SSH availability caveat; raised generation output cap to 8000 tokens for reasoning models; added a UI hint that the assistant knows the helpers
- [2026-07-17] [Fix] Bump Docker images and CI to Node 22 (pnpm 11.13.1 requires Node >=22.13); align corepack pnpm pin with packageManager
- [2026-07-17] [Fix] Node.js starter script template now awaits the async cronium helpers inside main() (previously called them synchronously, which returns unresolved Promises in container mode); removed the stray top-level input read
- [2026-07-17] [Docs] Corrected runtime-helper names in docs/Execution_Flow.md and docs/EVENTS.md to match the real SDKs: Python snake_case (get_variable/set_variable/set_condition), Bash cronium_get_variable/cronium_set_variable/cronium_set_condition, and noted helpers are auto-available (no require/import/source needed)
- [2026-07-17] [Chore] Bump pnpm to 11.14.0 (packageManager + Dockerfile corepack pins)
- [2026-07-17] [Fix] Remove unused CardDescription import from info-site MCP docs page (lint warning)
