# 2026-07-23

- [2026-07-23] [Testing] Phase 2 of the testing plan: 610 new tests for the API surface. tRPC routers tested through the real middleware chain via createCaller (jobs/quota-management/webhook-system/userAuth 100% stmts, workflows 99.6%, events 97.5%, variables 92.6%, tools 86.2%); all 17 orchestrator-facing /api/internal routes covered (92.6%) with response shapes pinned by shared fixtures in `tests/fixtures/internal-api/` for the future Go-side contract tests; public webhook, workflow-webhook, and socket-ticket routes at 100% with real HMAC signing. App coverage 15.31% → 24.98% statements (ratchet baseline raised).
- [2026-07-23] [Bug Fix] 12 bugs found and fixed during Phase 2 test writing (each with regression tests): workflows.update edges-only payload wiped the entire workflow graph; workflow connections dropped when a node DB id is 0; login timing oracle enabled account enumeration (bcrypt now always runs, incl. dummy-hash compare); forgotPassword revealed registered emails on SMTP-less instances; tools.executeAction/revokeOAuthTokens converted typed TRPC errors (rate limit, not-found) into 500s; tools.getAll tag search dead when a description existed; webhookSystem.getStats used invalid SQL (db.$count on a column) and failed on every call; webhookSystem.update silently re-enabled disabled webhooks and quotaManagement.updateUserQuota reset unspecified quotas (zod v4 .partial() fires field defaults — replaced with default-free update schemas); jobService.listJobs dropped falsy filters (priority LOW, eventId 0); workflow-webhook payload cap measured UTF-16 chars not bytes (~4MB bypass of the 1MB cap) and logged the plaintext bearer key; internal execution routes echoed raw error details in 500s, validated bodies before auth (validation oracle), and the internal servers route used un-awaited Next 15 params. Open findings tracked in `_plans/testing/FINDINGS.md`.
