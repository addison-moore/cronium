# 2026-08-06

- [2026-08-06] [Feature] MCP read/iterate tool surface (Phase 5 of `_plans/mcp/PLAN.md` v2): the MCP server grows from create-only (6 tools) to the full **build→run→inspect→fix→activate loop** (20 tools). New: `list_events`, `get_event`, `get_event_logs`, `list_workflows`, `get_workflow`, `get_executions`, `get_execution` (reads: summarized DTOs, limit/offset pagination, tail-truncated output via `maxOutputChars`, env-var KEYS only — values/credential secrets/webhook keys never returned), `update_event`, `update_workflow` (its `graph` replaces the node/edge set, synthesized server-side from `{events: [ids], connections: [{from,to,connectionType}]}` — no ReactFlow JSON authoring), `deactivate_event`, `delete_event`, `delete_workflow`, `run_event` (→ `{jobId, logId}`), `run_workflow` (→ `{executionId}`; runs work on DRAFT — the verify step). Every tool now carries MCP `annotations` (readOnly/destructive/idempotent hints) in `tools/list`; `create_workflow`/`update_workflow`/bundle surface a newly minted webhook key exactly once (previously lost); `get_capabilities` gains a `guidance` block teaching the iterate loop. The `mcp` token scope allowlist adds exactly the eight backing procedures (`events.update/deactivate/execute/getLogs`, `workflows.update/execute/getExecutions/getExecution`). Live-verified end-to-end on the dev instance: bundle → run (deliberately broken SQL) → failure surfaced via `get_execution`/`get_event_logs` (real root cause readable) → `update_event` fix → re-run 2/2 green (Slack posted) → graph replace, pause/activate, deletes; scoped token 403 outside the allowlist; secret-leak checks negative.
- [2026-08-06] [Refactor] The local stdio MCP server (`apps/cronium-mcp` 0.2.0) is now a **thin JSON-RPC bridge** to `/api/mcp` (~190 dependency-free lines: newline-delimited stdio framing, per-request forwarding with bearer auth, HTTP-failure→JSON-RPC error mapping naming the misconfigured env var, startup probe that fails fast on a bad token, in-flight drain on stdin close). Deletes the hand-rolled superjson tRPC client and per-tool registrations — the tool surface is now defined once in `apps/cronium-app/src/app/api/mcp/tools.ts` for both transports; drops the `@modelcontextprotocol/sdk`/`superjson`/`zod` runtime deps (kills the second-zod-copy CI risk class permanently).
- [2026-08-06] [Fix] Two pre-existing FK-order deletion bugs surfaced by MCP live-testing: `storage.deleteScript` and `storage.deleteUser` deleted `jobs` rows without first clearing `job_transitions` (a scheduling-overhaul table with no ON DELETE cascade), so deleting any event/user whose jobs had recorded transitions — i.e. practically anything ever executed — failed with a 23503 (UI delete path included). Both now clear `job_transitions` first, mirroring `retention.ts`; the storage FK-delete suite's seed graph (`tests/storage/helpers/seed.ts`) now includes a `job_transitions` row so the whole suite guards every jobs-deletion path (it previously passed while the live delete failed).
- [2026-08-06] [Test] MCP layer test coverage (Phase 6): contract tests for `POST /api/mcp` (JSON-RPC protocol: initialize/ping/notifications→202/batch/-32700/-32601/GET→405; auth 401s with RFC 9728 `WWW-Authenticate` discovery + `invalid_token` flagging; OAuth-before-API-token order; session/scopes/`requestSource:"mcp"` threading; tools/list schema+annotation exposure; handler failures as `isError` results not protocol errors), unit tests for the 20 tool handlers (summarization/no-script-bodies, pagination clamps, tail/head truncation, graph synthesis incl. duplicate/unknown-id rejection, bundle rollback, webhook-key one-time surfacing), `mcp` router tests through the real middleware chain (`getCapabilities` shape/normalization/no-secrets; `validatePlan` per error class: dup keys, schema, bad JSON, unknown toolId/type-mismatch/unknown action/param errors, unknown keys/self-loop/fan-in/cycle), and 6 node:test round-trip tests for the stdio bridge against a mock endpoint (`pnpm --filter @cronium/mcp-server test`). 62 new TS tests; coverage ratchet raised (statements 29.85→31.03, branches 26.15→27.66). App suite 1939 green; storage suite 26 green.
- [2026-08-06] [Feature] MCP evaluations (Phase 7, mcp-builder methodology): `apps/cronium-mcp/evals/` — idempotent `seed.mjs` builds the deterministic `EVAL-*` fixture set **through the MCP tools themselves** (8 events incl. cron/interval/HTTP fixtures; a 3-step ETL workflow run once → SUCCESS 3/3; an ON_FAILURE fallback chain run once → FAILURE with fallback executed), `eval.xml` with 10 independent read-only multi-tool questions (tags, cron lookup, graph successor/connection-type, step counts, error text, log output, execution status), every answer verified live via the tools, plus a README covering seeding/running/stability.
- [2026-08-06] [Docs] MCP doc-drift fixes (Phase 8): `apps/cronium-mcp/README.md` rewritten for the bridge (mandatory MCP-scoped tokens — scopes shipped 2026-07-17 and were still described as "planned"; grouped 20-tool table; remote-native connection note; removed dead plan references), `docs/MCP.md` rewritten (one surface/two transports, tool groups, mandatory-scopes reality replacing the stale "null = full rights", secret-hygiene guarantees, test map), stale `token-scopes.ts` header corrected (null = cookie sessions only), and the info-site `/docs/mcp` page updated (full-loop narrative, grouped tool list, runs-are-explicit safety bullet, scope+expiry wording, bridge framing).
