# 2026-07-22

- [2026-07-22] [Security] Phase 4.2: a `knip` static-inventory pass surfaced four more genuinely dead dependencies — `@slack/web-api`, `cmdk`, `express-session`, `express-ws` (+ `@types/express-session`, `@types/express-ws`) — with zero source imports; removed them (typecheck + build verified).

- [2026-07-22] [Security] Phase 4.2: removed the unused `@tanstack/react-query-devtools` from production deps and moved the build/dev-only `tailwindcss` and `tsx` to devDependencies (production runs compiled `.cjs`/Next standalone, so neither is a runtime dependency). App build verified.

- [2026-07-22] [Security] Phase 4.2: removed confirmed dead dependencies from cronium-app (`@faker-js/faker`, `passport` + `passport-local` + their `@types`, `@dnd-kit/core|sortable|utilities`) and deleted the orphaned `src/lib/passport.ts` (a `LocalStrategy` module nothing imported). 13 packages pruned; typecheck + app build verified.

- [2026-07-22] [Security] Phase 4.3: hardened the public marketing contact endpoint — 16 KB body cap before parsing, strict field validation with length limits (subject/email/message), real email-shape check, per-IP fixed-window rate limit, a honeypot field (hidden in the form) that silently drops bots, SMTP connection/socket timeouts + a pooled reusable transport with bounded concurrency, and generic public error responses (detail stays in server logs).

- [2026-07-22] [Security] Phase 4.1: split `POST /api/admin/integrity` mutations (reconcile/fix-unlinked/cleanup) out of the old mutating GET into POST behind a new same-origin (CSRF) + admin-MFA + audit guard (`admin-mutation.ts`); applied the same guard + audit to `POST /api/admin/cleanup`; stopped leaking `error.message` from those routes. Deleted the orphaned `seed-db.ts` (hardcoded `admin`/`admin` default-credential seeder with no production guard).
- [2026-07-22] [Security] Phase 4.1: hardened the unauthenticated MCP OAuth Dynamic Client Registration endpoint — per-IP fail-closed rate limit, 8 KB body cap, and caps on `redirect_uris` count/length and `client_name` length; registrations now expire after 30 days of disuse (sliding TTL) instead of persisting forever.
- [2026-07-22] [Security] Phase 4.3: the template processor's `lookup` helper now rejects `__proto__`/`constructor`/`prototype` and non-own keys (added regression test), and the misleading "disable prototype pollution protection" comment was corrected to a warning.

- [2026-07-22] [Security] Phase 4.3: applied the browser security-header baseline to the info/marketing site (`X-Content-Type-Options`, `X-Frame-Options: DENY`, `Referrer-Policy`, `Permissions-Policy`, production `HSTS`, and a CSP with `frame-ancestors 'none'`/`object-src 'none'`/`base-uri 'self'`/`form-action 'self'`) via `next.config` `headers()`. Build verified.

- [2026-07-22] [Security] Phase 3.4: removed the globally pre-installed `axios@1.6.2` from the Node.js execution image (both Dockerfiles) and dropped the circular `require.resolve("axios")` check from its health check. The Cronium SDK uses only Node built-ins and Node 20 ships a global `fetch()`. USER-FACING: scripts that did `require("axios")` in the Node runtime must switch to `fetch()` or `npm install` axios themselves.

- [2026-07-22] [Security] Phase 3.5/3.4: resolved all high/critical dependency advisories via pnpm-workspace overrides (`sharp` ≥0.35.3 for the libvips CVEs, `fast-uri` ≥3.1.4, `@hono/node-server` ≥2.0.5 — all transitive through Next/MCP SDK) and added a blocking `pnpm audit --audit-level high` CI gate. Added SBOM + provenance (`mode=max`) attestations to the three published Docker images (cosign keyless signing + Trivy SARIF were already in place).

- [2026-07-22] [Security] Phase 3.6: the orchestrator now fails closed on a plaintext remote Docker endpoint — only a local `unix://`/`npipe://` socket is allowed (no mutual TLS is wired, so a `tcp://` endpoint would expose an unauthenticated Docker API). An operator can override with `CRONIUM_ALLOW_INSECURE_DOCKER_ENDPOINT=true` on a trusted network.

- [2026-07-22] [Security] Phase 3.5: added a CodeQL SAST workflow (TypeScript + Go, `build-mode: none`) on push/PR/weekly-schedule, with actions pinned to SHAs.

- [2026-07-22] [Security] Phase 3.6: excluded secret material (`*.pem`, `*.key`, `known_hosts`, `data/`, `*.bak`, `backups/`) and planning/scratch notes (`_plans`, `_scratch`) from the Docker build context via `.dockerignore`.

- [2026-07-22] [Security] Phase 3.4: aligned the Go toolchain to a single pinned patched version (go 1.25.12) across `go.work`, all three `go.mod` files, CI `setup-go`, and the Docker builders (was 1.22/1.23/1.25 mixed); this clears all reachable Go standard-library vulnerabilities. Added a `govulncheck` gate to CI (per Go module) and a PR `dependency-review` job.

- [2026-07-22] [Security] Phase 3.5: pinned every GitHub Action to a reviewed commit SHA (notably `aquasecurity/trivy-action@master` → v0.36.0 SHA), and made `install.sh` fail closed when no release is discovered instead of pulling a mutable `main`/`latest` image (still allowed under `--no-pull` for local images).

- [2026-07-22] [Security] Phase 3.2: the dormant `POST /api/admin/terminal` local-host command path (arbitrary RCE on the control plane, no UI caller) is now disabled by default. It requires an explicit `CRONIUM_ALLOW_LOCAL_HOST_TERMINAL=true` opt-in and, when enabled, an Admin role + MFA-enabled account, and audits every command. Remote (serverId) SSH execution is unchanged.

- [2026-07-22] [Security] Phase 3.6: production Valkey now runs with `requirepass` (new `VALKEY_PASSWORD`, wired through app/worker via URL, runtime via `RUNTIME_VALKEY_PASSWORD`, and per-job sidecars via new orchestrator `CRONIUM_CONTAINER_RUNTIME_VALKEY_PASSWORD` injection); Postgres/Valkey host port publishes in `infra/docker/docker-compose.yml` bound to loopback. install.sh generates `VALKEY_PASSWORD`; deploy docs/env examples/info-site/install-smoke updated.
