# Changelog - 2026-07-07

- [2026-07-07] [Documentation] Conducted a comprehensive release-readiness review of the entire monorepo (code completeness, integrations, security, repo hygiene, docs, CI, and \_plans status) and created `_plans/RELEASE_READINESS_REPORT.md` with a prioritized P0–P3 roadmap of remaining work before public release.
- [2026-07-07] [Security] Implemented SSH host key verification in the orchestrator (known_hosts-backed, `hostKeyPolicy`: accept-new/strict/insecure), replacing `ssh.InsecureIgnoreHostKey()`; added unit tests.
- [2026-07-07] [Security] Implemented Ed25519 payload signing end-to-end: orchestrator auto-generates a signing key, signs every payload, and uploads the `.sig`; the runner verifies the signature via `CRONIUM_VERIFY_KEY` before execution; added unit tests in both modules.
- [2026-07-07] [Bug Fix] Fixed `pnpm lint` (bad lint-go.sh path in apps/runtime; ESLint 9 flat config for packages/ui plus 12 lint fixes) and `pnpm test` (SKIP_ENV_VALIDATION for Jest, `--passWithNoTests`); all root harness commands now pass.
- [2026-07-07] [Bug Fix] Fixed the broken deployment path: `deploy.sh`/`setup-dev.sh` no longer reference the nonexistent `docker-compose.stack.yml`; `deploy.sh` builds CI-parity GHCR-tagged images; `infra/docker/docker-compose.yml` rewritten with correct build contexts, GHCR image names, and the missing runtime service.
- [2026-07-07] [Feature] Bootstrap seeding polish: `bootstrap-seed.cjs` now encrypts the seeded `smtpPassword` (matching the admin UI's AES-256-GCM format) and redacts it from logs; `docker-compose.example.yml` enables `AUTO_SEED_ADMIN` so a fresh install is immediately loginable.
- [2026-07-07] [Documentation] Fixed licensing: populated the empty `LICENSE-AGPL.md` with the full AGPL-3.0 text and standardized `AGPL-3.0-or-later` across all package.json manifests; unified the repo URL in COMMERCIAL-LICENSE.md; documented `CRONIUM_SSH_SECURITY_*` env vars; corrected CLAUDE.md security claims.
- [2026-07-07] [Refactor] Deleted `_scratch/.env` (untracked file containing live-looking credentials — rotation required, see `_plans/p0-release-blockers/PLAN.md` Outcome); fixed a `go vet` IPv6 address-format issue in the SSH tunnel.
- [2026-07-07] [Documentation] Planned the P1 "features that lie" milestone: created `_plans/p1-features-that-lie/PLAN.md` (webhook mock removal + real inbound trigger payload/rate-limiting, real email plugin sends, real tool connection tests, admin test-email button, monitoring truth-pass, admin roles persistence + enforcement, OAuth token bridge + Google Sheets completion). Awaiting review before implementation.
